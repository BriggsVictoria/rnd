---
slug: client/projects/delete
---
{% query_graph 'current_user', result_name: 'g' %}
{% query_graph 'get_project', result_name: 'graph_project', slug: params.slugs, creator_id: g.current_user.id %}
{% if graph_project %}
  <h1>Edit {{ graph_project.project.id }}</h1>
  {% include_form 'destroy_project', resource_id: graph_project.project.id %}
{% else %}
  <p>Unfortunately we could not find this project.</p>
{% endif %}